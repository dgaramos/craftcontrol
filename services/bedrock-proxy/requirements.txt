# Host agent has no third-party runtime dependencies.
# It uses only the Python standard library (http.server, socket, subprocess, threading).
#
# Development / testing dependencies:
pytest>=8.0
pytest-cov>=6.0
