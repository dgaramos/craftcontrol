#!/usr/bin/env bash
set -euo pipefail

readonly ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

for gate in frontend backend host-agent contracts contracts-frontend integration; do
  "bin/check-$gate"
done

git diff --check
echo "complete quality gate: ok"
