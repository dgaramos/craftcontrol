#!/usr/bin/env bash
set -euo pipefail

readonly ROOT="$(git rev-parse --show-toplevel)"
readonly PYTHON="${PYTHON:-python3}"
cd "$ROOT"

"$PYTHON" -m pytest apps/server/tests/test_agent_workflows.py -v
echo "agent-workflows gate: ok"
