.env
.claude/*
!.claude/agents/
.agents/*
!.agents/skills/
__pycache__/
*.py[cod]
.pytest_cache/
data/*.db
data/backups/
!data/.gitkeep
roadmap/

# Coverage
.coverage
coverage.xml

# Frontend
# Anywhere: apps/client, packs/*, and any workspace added later. The previous
# rule was pinned to apps/frontend/, which stopped matching when that directory
# was renamed to apps/client — and 5757 files were committed before anyone
# noticed. package-lock.json is the source of truth; `npm ci` recreates these.
node_modules/
test-results/
.coverage *
apps/client/coverage/
apps/client/junit.xml
apps/server/.coverage *
apps/server/minecraft_manager/
