#!/usr/bin/env bash
set -euo pipefail

readonly ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

for gate in frontend backend bedrock-proxy contracts contracts-frontend dr-agents deploy integration; do
  "bin/check-$gate"
done

git diff --check
echo "complete quality gate: ok"
